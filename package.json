{
	"name": "chkip",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"gen": "node mappings-gen.js",
    	"dev": "npm run gen && wrangler dev",
    	"dev_remote": "npm run gen && wrangler dev --remote",
    	"deploy": "npm run gen && wrangler deploy",
		"push": "git add . && git commit -m \"$npm_config_message\" && git push origin main"
    },
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"vitest": "~3.2.0",
		"wrangler": "^4.75.0"
	}
}